import { test } from '../../../../fixtures/frameworkFixtures';

test.describe('Accounts Management - Account Statements', { tag: ['@portal', '@accounts', '@account-statements', '@regression'] }, () => {
  test.skip(
    'AM-TC-013/014/015/016 - Account statements require confirmed navigation, date controls, and download locator inventory',
    async () => {}
  );
});
