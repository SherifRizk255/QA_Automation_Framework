import { test } from '../../../../fixtures/frameworkFixtures';

test.describe('Accounts Management - Account Identifier View', { tag: ['@portal', '@accounts', '@account-identifier-view', '@regression'] }, () => {
  test.skip(
    'AM-TC-005/006/007 - Full account identifier view requires confirmed details screen and masking rules',
    async () => {}
  );
});
